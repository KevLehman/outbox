const jwt = require('json-web-token');

const createJWT = (userData, permissions) =>{};

const validateJWt = (token) => {} 

const hasEnoughPermissions = (userData, permissions) => {};

module.exports = {
  createJWT,
  validateJWt,
  hasEnoughPermissions,
};
